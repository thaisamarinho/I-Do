<%= form_tag wedding_path(@wedding), method: "get",
                                     id: "form-group",
                                     remote: true do %>
  <%= text_field_tag :search, params[:search] %>
  <%= button_tag do %>
    <span class="glyphicon glyphicon-search search"></span>
  <% end %>
<% end %>
